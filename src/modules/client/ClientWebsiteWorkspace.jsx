import { Layout } from '../../layouts/Shell.jsx'
import { mediaItems } from '../../services/mockData.js'
import { navigationItems, seoItems, websiteHealth, websitePages, websiteWorkspace } from '../../services/websiteManager.js'

export function ClientWebsiteWorkspace() {
  return <Layout client title="My Website"><section className="clientSyncHero card websiteManagerHero"><div><span>Website Hub</span><h2>{websiteWorkspace.name}</h2><p>Everything the client needs is here: edit pages, manage media, check SEO, save drafts and request publish.</p></div><div className="repoCard clientSummary"><b>{websiteWorkspace.status}</b><small>{websiteWorkspace.domain}</small><small>No developer tools shown</small><a href="/client/editor">Edit Website</a></div></section><div className="clientHealthGrid simpleHealth">{websiteHealth.map(([label, value, help]) => <article className="card clientHealthCard" key={label}><span>{label}</span><strong>{value}</strong><small>{help}</small></article>)}</div><section className="simpleWebsiteGrid"><div className="card managerPanel mainWork"><div className="panelHead"><h2>Pages</h2><button onClick={() => location.href='/client/editor'}>Edit Content</button></div>{websitePages.map(page => <article className="simplePageRow" key={page.title}><div><b>{page.title}</b><small>{page.slug} · {page.draft}</small></div><span>{page.status}</span><button onClick={() => location.href='/client/editor'}>Edit</button></article>)}</div><aside className="card managerPanel nextSteps"><h2>What do you want to do?</h2><button onClick={() => location.href='/client/editor'}>Edit website text</button><button onClick={() => location.href='/client/media'}>Upload images</button><button onClick={() => location.href='/client/publish'}>Request publish</button><button onClick={() => location.href='/client/support'}>Ask KSJ for help</button></aside></section><section className="simpleWebsiteGrid"><div className="card managerPanel"><div className="panelHead"><h2>Media</h2><button onClick={() => location.href='/client/media'}>Open Media</button></div><div className="miniMediaGrid">{mediaItems.slice(0,6).map(item => <article key={item}><b>{item.slice(0,2).toUpperCase()}</b><span>{item}</span></article>)}</div></div><div className="card managerPanel"><div className="panelHead"><h2>Navigation</h2><button>Manage</button></div>{navigationItems.slice(0,6).map((item, index) => <article className="navManagerRow" key={item[0]}><b>0{index + 1}</b><div><strong>{item[0]}</strong><small>{item[1]}</small></div><span>{item[2]}</span></article>)}</div></section><section className="simpleWebsiteGrid"><div className="card managerPanel"><div className="panelHead"><h2>SEO Basics</h2><button>Update</button></div>{seoItems.map(item => <article className="seoManagerRow" key={item[0]}><div><b>{item[0]}</b><small>{item[1]}</small></div><span>{item[2]}</span></article>)}</div><div className="card managerPanel publishBox"><h2>Publishing</h2><p>Clients never push changes live directly. They save a draft, request publish, and KSJ Digital reviews it first.</p><div className="publishSteps"><span>1. Edit content</span><span>2. Save draft</span><span>3. Request publish</span><span>4. KSJ approves</span></div><button onClick={() => location.href='/client/publish'}>Go To Publish</button></div></section></Layout>
}
